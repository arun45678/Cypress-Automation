{
  "name": "cypressautomation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run",
    "headTest": "npx cypress run --headed",
    "ChromeTest": "npx cypress run --browser chrome --headed",
    "Chrometestfeaturespec": "npx cypress run --spec \"cypress/integration/examples/BDD Framework/*.feature\" --headed --browser chrome",
    "Chrometestfeaturespec2": "npx cypress run --spec \"cypress/integration/examples/BDD Framework/ecom.feature\" --headed --browser chrome"
  },
  "cypress-cucumber-preprocessor": {
    "json": {
      "enabled": true,
      "output": "cypress/cucumberReports/results.json"
    }
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.40.2",
    "allure-commandline": "^2.32.0",
    "cypress": "^13.16.1",
    "cypress-iframe": "^1.0.1",
    "cypress-mochawesome-reporter": "^3.8.2",
    "cypress-xpath": "^2.0.1",
    "multiple-cucumber-html-reporter": "^3.8.0"
  },
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^21.0.2",
    "@cypress/browserify-preprocessor": "latest",
    "cypress-iframe": "^1.0.1"
  }
}
